<Window x:Class="DoomNet.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:blazor="clr-namespace:Microsoft.AspNetCore.Components.WebView.Wpf;assembly=Microsoft.AspNetCore.Components.WebView.Wpf"
        xmlns:local="clr-namespace:DoomNet.Components"
        Title="DoomNet - FPS Game" Height="800" Width="1200"
        WindowState="Maximized"
        Background="#1a1a1a">
    <Grid>
        <!-- Blazor WebView untuk merender UI Game dan Three.js -->
        <blazor:BlazorWebView x:Name="webView1" HostPage="wwwroot/index.html" Services="{DynamicResource services}">
            <blazor:BlazorWebView.RootComponents>
                <blazor:RootComponent Selector="#app" ComponentType="{x:Type local:Routes}" />
            </blazor:BlazorWebView.RootComponents>
        </blazor:BlazorWebView>
        
        <!-- Overlay sederhana untuk debugging atau UI natives jika diperlukan -->
        <TextBlock x:Name="StatusText" 
                   Text="DoomNet Loaded" 
                   Foreground="LimeGreen" 
                   FontSize="12" 
                   Margin="10" 
                   VerticalAlignment="Bottom" 
                   HorizontalAlignment="Left"
                   Opacity="0.7"/>
    </Grid>
</Window>
